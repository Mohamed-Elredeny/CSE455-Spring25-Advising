apiVersion: v1
kind: Service
metadata:
  name: course-service
  labels:
    app: course-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: course-service 